resources:
  - namespace.yaml
  - network-policies/allow-egress.yaml
  - network-policies/allow-external-ingress.yaml
  - network-policies/default-deny-ingress.yaml
  - populate-rds-job.yaml

configMapGenerator:
  - name: PROJECT_GROUP_SHORT_TO_REPLACE-PROJECT_NAME_SHORT_TO_REPLACE-K8S_JOB_NAME_TO_REPLACE-configmap
    literals:
      - PROFILE=PROFILE_TO_REPLACE
      - DB_NAME=DB_NAME_TO_REPLACE
      - DB_HOST=DB_HOST_TO_REPLACE
      - DB_USERNAME=DB_USERNAME_TO_REPLACE
      - DB_PASSWORD=DB_PASSWORD_TO_REPLACE

images:
  - name: data
    newName: 461183108257.dkr.ecr.eu-west-2.amazonaws.com/uec/dos/test-database/data
    newTag: DOS_TEST_DATABASE_TAG_TO_REPLACE
